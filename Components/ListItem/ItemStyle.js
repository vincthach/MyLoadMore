import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imageStyle: {
    marginLeft: 40,
    marginRight: 20
  },
  textStyle: {
    flex: 1,

    fontSize: 20
  }
});

export default styles;
